# You probably do not need this file at all any more.
#
# The zone id is now a default in variables.tf (it is not secret), and both API
# tokens are read from the environment:
#
#   export CLOUDFLARE_API_TOKEN=...
#   export PROXMOX_VE_API_TOKEN=...
#
# Copy this to terraform.tfvars (gitignored) only to override a default.

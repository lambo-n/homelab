# --- SECRET MATERIAL — never commit ---
age.key
*.agekey
kubeconfig
# Local SSH client config for this VM's ProxyJump aliases; per-machine, not repo state.
ssh-config
*.decrypted.yaml
*.plaintext.yaml

# Only *.sops.yaml (encrypted) belongs in this repo. A bare secret.yaml is
# plaintext by definition, so it is refused outright.
secret.yaml
**/secret.yaml
worker-credentials.yaml
**/worker-credentials.yaml

# --- tooling ---
.mise/
.direnv/

# Byproduct of running the CI helper in .github/scripts/ locally.
__pycache__/
*.pyc

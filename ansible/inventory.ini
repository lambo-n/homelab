# Connection details (host, user, key) live in ~/.ssh/config as the
# pve-hostconfig alias -- per-machine, gitignored, not repo state. This file
# only says which SSH alias belongs to which role group.
[proxmox]
pve-hostconfig

# Install: GPU-VM.md F6b. Prometheus scrapes node_exporter every 60 s; 15 s keeps
# every scrape at most 15 s behind.
[Unit]
Description=Collect llama-server metrics every 15 s

[Timer]
OnBootSec=30s
OnUnitActiveSec=15s
AccuracySec=1s

[Install]
WantedBy=timers.target

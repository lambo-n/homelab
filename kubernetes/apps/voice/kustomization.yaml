apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Voice assistant: Home Assistant as the pipeline orchestrator for the ESP32-S3
# satellite. Speech-to-text and text-to-speech run on VM 105, not here -- see
# VOICE.md for the split and why.
resources:
  - namespace.yaml
  - voice-db/ks.yaml
  - home-assistant/ks.yaml

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - configmap.yaml
  - credentials.sops.yaml
  - deployment.yaml
  # Kept after the switch to local management: nothing references it now, but it
  # is the rollback path back to `run --token`. Removing it is a later decision.
  - secret.sops.yaml
